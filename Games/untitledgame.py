import turtle

screen = turtle.Screen
screen.title("untitled game")
screen.bgcolor("lightblue")

untitled1 = turtle.Turtle
untitled1.shape("rectangle")
untitled1.color("lightblue")
untitled1.shapesize(1, 3)
untitled1.penup()
untitled1.goto(0, 0)

if user == "a"
untitled1.goto(0, -1)

if user == "d"
untitled1.goto(0, 1)

if user == "s"
untitled1.goto(-1, 0)

if user == "w"
untitled1.goto(1, 0)

if user == "q"
untitled1.goto(1000, 1000)


if user == "exit"
open(/ExbosSahara/main.py)
